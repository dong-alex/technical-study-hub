// validates inputs for questions - backend will have validators
import React, { useEffect, useState } from "react";
import validator from "validator";
import { Tag, QuestionValidatorState } from "../types";

const useQuestionValidator = () => {};

export default useQuestionValidator;
